const movieHeader = document.getElementById("movie-header-title");
const movieContent = document.getElementById("movie-content");

/**
 * Render a specific movie.
 * @param {any} movie - Movie JSON object.
 */
const renderMovie = (movie) =>
{
	
};
